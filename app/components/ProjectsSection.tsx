import type { Project } from '../types';
import { Field, styles } from './common';
export function ProjectsSection(props: { projects: Project[]; currentProjectId: string | null; newProjectName: string; newProjectDescription: string; newProjectManager: string; setNewProjectName: (v: string) => void; setNewProjectDescription: (v: string) => void; setNewProjectManager: (v: string) => void; addProject: () => void; setActiveProject: (id: string) => void; renameProject: (id: string) => void; updateProjectMeta: (id: string) => void; deleteProject: (id: string) => void; }) {
  return <div><h2 style={styles.sectionTitle}>פרויקטים</h2><div style={styles.formGrid}><Field label="שם פרויקט"><input style={styles.input} value={props.newProjectName} onChange={(e) => props.setNewProjectName(e.target.value)} /></Field><Field label="מנהל פרויקט"><input style={styles.input} value={props.newProjectManager} onChange={(e) => props.setNewProjectManager(e.target.value)} /></Field><Field label="תיאור" full><textarea style={styles.textarea} value={props.newProjectDescription} onChange={(e) => props.setNewProjectDescription(e.target.value)} /></Field></div><div style={styles.buttonRow}><button style={styles.primaryBtn} onClick={props.addProject}>הוסף פרויקט</button></div><div style={{ ...styles.cardGrid, marginTop: 18 }}>{props.projects.map((project) => <div key={project.id} style={styles.recordCard}><div style={{ display: 'flex', justifyContent: 'space-between', gap: 8 }}><div style={{ fontWeight: 800 }}>{project.name}</div>{project.id === props.currentProjectId && <span style={{ background: '#dcfce7', color: '#166534', padding: '4px 10px', borderRadius: 999 }}>פעיל</span>}</div><div style={{ color: '#475569', marginTop: 8 }}>מנהל: {project.manager || '-'}</div><div style={{ color: '#475569', marginTop: 4 }}>תיאור: {project.description || '-'}</div><div style={{ color: '#475569', marginTop: 4 }}>נוצר: {project.createdAt}</div><div style={styles.buttonRow}><button style={styles.secondaryBtn} onClick={() => props.setActiveProject(project.id)}>בחר</button><button style={styles.secondaryBtn} onClick={() => props.renameProject(project.id)}>ערוך שם</button><button style={styles.secondaryBtn} onClick={() => props.updateProjectMeta(project.id)}>ערוך פרטים</button><button style={styles.dangerBtn} onClick={() => props.deleteProject(project.id)}>מחק</button></div></div>)}</div></div>;
}
