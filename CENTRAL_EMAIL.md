# מנגנון מייל מרכזי

הממשק נמצא בכפתור "שליחה במייל / היסטוריה". מסכי RFI, בקרות מקדימות ודוחות פיקוח משתמשים באותו חלון. גם מסמכי איכות ונקודות עצירה מציעים פעולה על כל רשומה. הקבצים המשויכים לרשומה מסומנים מראש. PDF של הטופס ניתן להפיק מתוך החלון, בנפרד מהנספחים, כדי לאפשר הסרה ובחירה של כל קובץ. ניתן להוסיף קבצים מהמחשב, לבחור תבנית, לערוך את כל שדות המייל ולבדוק תצוגה מקדימה לפני שליחה.

## הפעלה בסביבה

1. יש לוודא שהותקנו `app/supabase/05_supabase_auth_rls.sql` ו-`09_project_email_users.sql`, ושמשתמשי Supabase משויכים לפרויקטים ב-`project_members` עם `active=true` ותפקיד מתאים. מזהי הפרויקטים צריכים להיות מזהי UUID הקיימים בטבלת projects.
2. להריץ במסד הנתונים את `app/supabase/10_central_email.sql`. העדכון יוצר היסטוריה המנוהלת רק בשרת, ומחליף גישה אנונימית לתיבות הדואר בהרשאות פרויקט. ניהול חשבונות הדואר מוגבל למנהלי הפרויקט.
3. להגדיר בשרת בלבד `SUPABASE_SERVICE_ROLE_KEY`, נוסף להגדרות Supabase הקיימות. אין להוסיף תחילית NEXT_PUBLIC למפתח זה ואין להכניסו לקוד.
4. יש להגדיר משתמש פרויקט פעיל בתפקיד בקר איכות עם כתובת Gmail וסיסמת אפליקציה. פרטי השולח נשלפים בשרת מטבלת project_email_users; סיסמה בבקשת שליחה אינה מתקבלת מהלקוח.
5. לפרוס את הקוד וההגדרות יחד עם המיגרציה. כניסה מקומית ישנה אינה מספיקה לשליחת מייל; המשתמש חייב להתחבר באמצעות Supabase. לאחר ההפעלה, לבצע בדיקת קבלה לתיבה מורשית לפי בחירת מנהל המערכת.

לא בוצעו במסגרת בדיקות הפיתוח שליחות אמיתיות, שינוי במסד הנתונים המרוחק או פריסה. בסביבת הפיתוח שנבדקה היו רק משתני Supabase ציבוריים; מפתח השרת לא הוגדר.

## חוזה שימוש חוזר

`EmailComposer` מקבל `MailContext` מתוך `app/lib/email.ts`: מזהה פרויקט, מזהה מודול חופשי, מזהה רשומה, כותרת, נתונים לשילוב בתבנית וקבצים. `recordIds` מאפשר שיוך של משלוח מרובה רשומות להיסטוריה של כל אחת מהן. `generateDocuments` הוא מתאם אופציונלי להפקת PDF קיים של המודול. אין רשימה סגורה של סוגי מודולים בשרת. מודול חדש יכול להפעיל אותו חלון באמצעות נתוני הרשומה שלו.

`collectMailAttachments` תומך במבני הקבצים המקוננים של המערכת, לרבות dataUrl, attachmentDataUrl, fileDataUrl, fileUrl, url ו-contentBase64. הוא מתעלם משדות חתימות וסיסמאות ומסיר כפילויות. מסמכים קשורים הנשמרים מחוץ לרשומה צריכים להימסר במפורש למתאם של המודול. מסכים ללא רשומה פעילה מציעים שליחה והיסטוריה בהקשר הפרויקט.

שתי כתובות השרת הקיימות משתמשות באותו שירות: `/api/send-email` ו-`/api/send-checklist-email`. בקשה דורשת Bearer token, projectId, module, recordId, requestId, senderEmail, to, subject, text ו-attachments. רשימת הנספחים יכולה להיות ריקה. cc/bcc אופציונליים. כל ניסיון צריך מזהה UUID; אין לשנות מזהה ולנסות אוטומטית שוב לאחר כשל תקשורת.

## מגבלות וסטטוסים

- עד 100 נמענים, 30 קבצים ו-3MB תוכן קבצים יחד, כדי להישאר מתחת למגבלת גוף הבקשה בפריסה הקיימת. קבצים גדולים דורשים הרחבה עתידית להעלאה נפרדת; אין המרה שקטה לקישורים.
- קבצים מרוחקים נטענים בדפדפן מאחסון Supabase המוגדר בלבד. השרת אינו ניגש לקישורים או נתיבי קבצים מהבקשה. כשל בטעינת קובץ נבחר עוצר את השליחה.
- `sent`: התקבל בשרת הדואר, לא אישור מסירה או קריאה. `partial`: חלק מהנמענים נדחו. `failed`: השליחה נכשלה. `unknown`: כשל תקשורת שלא מאפשר לקבוע אם נשלח. `sending`: ניסיון שתועד ועדיין אין לו תוצאה סופית, לרבות הפסקת תהליך.
- תיעוד ההתחלה חייב להצליח לפני שליחה. אם עדכון התוצאה נכשל לאחר השליחה, הממשק מציג אזהרה ולא מאפשר שליחה חוזרת באותו חלון. בקשות כפולות נחסמות באמצעות מזהה ייחודי במסד הנתונים.
- היסטוריה מציגה עד 50 ניסיונות אחרונים לרשומה או לפרויקט. נמעני BCC אינם מוחזרים בתצוגת ההיסטוריה המשותפת. טיוטות ללא מזהה שמור מקבלות מזהה עצמאי, והיסטורייתן זמינה בהיסטוריית הפרויקט.

## אימות

`node --test tests/central-email.test.cjs` מפעיל בדיקות עם SMTP ומסד נתונים מדומים בלבד. נבדקים הרשאות, שילוב נתונים, נספחים, ולידציה, תיעוד, שליחה כפולה, דחייה חלקית וכשל תקשורת. השוואת TypeScript למצב המקורי הראתה 100 שגיאות קיימות ללא שגיאות חדשות. הפרויקט מוגדר מראש לדלג על שגיאות TypeScript בבנייה.
